import { RestaurantFeedPipe } from './restaurant-feed.pipe';

describe('RestaurantFeedPipe', () => {
  it('create an instance', () => {
    const pipe = new RestaurantFeedPipe();
    expect(pipe).toBeTruthy();
  });
});
